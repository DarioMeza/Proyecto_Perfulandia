spring.application.name=ProductosService

# Puerto en el que correrá este microservicio
server.port=8081

# Conexión a base de datos MySQL en Railway
spring.datasource.url=jdbc:mysql://yamanote.proxy.rlwy.net:36120/railway
spring.datasource.username=root
spring.datasource.password=ENunnSPvKWgasoVBNcSFpKJMeDbKdZrH
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#-------------------------------------
# Título que aparecerá en la interfaz de Swagger UI
springdoc.swagger-ui.title=Documentación API Productos

# Ruta donde se exponen los documentos OpenAPI (JSON)
springdoc.api-docs.path=/api-docs

# Ruta donde está disponible la interfaz gráfica Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html

